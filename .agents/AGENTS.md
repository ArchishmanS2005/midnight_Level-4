# Workspace Rules

- **Do NOT run npm/npx/build/test commands on Windows host shell.**
- Only edit files directly.
- Ask the user to run `npm` / `deploy` / test commands inside WSL/Linux and paste the output.
